-- Part A)
--   ∃ A ∈ AA, x ∉ ⋃₀ AA → x ∉ A
-- and
--   ∃ A ∈ AA, x ∉ A → x ∉ ⋃₀ AA.

-- Part B)
--   ∀ A ∈ AA, x ∉ ⋃₀ AA → x ∉ A
-- and
--   ∀ A ∈ AA, x ∉ A → x ∉ ⋃₀ AA.

-- Part C)
--   ∃ A ∈ AA, x ∉ ⋂₀ AA → x ∉ A
-- and
--   ∃ A ∈ AA, x ∉ A → x ∉ ⋂₀ AA.

-- Part D)
--   ∀ A ∈ AA, x ∉ ⋂₀ AA → x ∉ A
-- and
--   ∀ A ∈ AA, x ∉ A → x ∉ ⋂₀ AA.
